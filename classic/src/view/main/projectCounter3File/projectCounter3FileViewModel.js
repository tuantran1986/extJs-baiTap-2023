Ext.define('MyApp.view.main.projectCounter3File.projectCounter3FileViewModel', {
    extend: 'Ext.app.ViewModel',
    alias: 'viewmodel.projectCounter3FileViewModel',

    // khai báo - dữ liệu MODEL = biến "myTitle"
    data: {
        myTitle: '3. My Title'
    }
})